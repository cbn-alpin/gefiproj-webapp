export enum GenericTableCellType {
  NUMBER = 'number',
  TEXT = 'string',
  DATE = 'date',
  CURRENCY = 'currency',
  NEGATIVECURRENCY = 'negative-currency',
  BOOLEAN = 'boolean',
  SELECTBOX = 'selectbox',
  TEXTAREA = 'textarea'
}
